"""Legacy nurturing workflow — kept for historical reference only.

The active workflow is in ``nurturing/workflow_v2.py`` and uses a
LangGraph state machine. The v1 single-agent workflow previously lived
in ``nurturing/workflow.py`` and was moved here in 2026 during the
AI Radar rework (replaced by researcher → scorer → writer →
chart_planner nodes with HEAD-validated URLs).
"""
